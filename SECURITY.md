# 安全说明

请勿在公开 Issue、截图或日志中提交 API Key、OAuth token、auth.json、加密账号库、恢复记录或会话内容。报告问题只需描述版本、操作步骤和脱敏错误。

## 信任边界

- 本工具和官方客户端以当前 Windows 用户权限运行。DPAPI 保护静态保存的凭据，不防御已取得该用户权限的软件。
- 共享目录的活动凭据由 Codex 读取；恢复日志包含敏感快照，经当前用户 DPAPI 加密。不要删除未处理的 `pending-*.dpapi`。
- API 档决定后续请求发往哪里。恢复旧会话可能将历史内容发送给新的 API 服务商；添加前确认其可信度和数据处理条款。
- 输入的 Key 通过子进程标准输入传递，而非命令行参数。测试连接可能计费，必须显式确认。
- 仅支持每次一个共享目录使用者。事务支持进程中断恢复，但不是文件系统跨文件原子事务，无法保证断电、磁盘损坏或恶意并发时的数据完整性。
- 不复制其他用户账号、不绕过服务权限、不提供账号池或额度。

## 可选 Astra 预热

- 默认关闭，按 API 地址和 Key 的组合明确授权费用。只发送固定短请求，不将原消息、附件、工具或工作区内容发送给预热模型。
- 启用/关闭会维护默认用户目录中的 `hooks.json`，保留其他 hook 并备份修改前内容。用户必须在官方 Codex 内亲自审查与信任；工具不会写入信任批准记录。
- 授权仅保存 SHA256 指纹，成功状态仅按账号与会话记录。不额外保存明文 Key、请求原文或响应内容；hook 运行时从活动文件登录读取凭据。
- 仅支持内置 `openai` 路由。拒绝重定向，正常验证 HTTPS，仅本机回环允许 HTTP。网络总期限 20 秒、响应上限 128 KiB；失败拦截原消息，不自动重试或更换 Astra 模型。
- 实验功能不保证服务商的冷启动效果。卸载或移动程序前，关闭所有已启用 API 档的预热，避免残留失效 hook。升级后按官方要求重新审查 hook。

## macOS 版

- macOS 使用 AES-256-GCM 加密账号库和恢复记录，密钥在本机登录钥匙串，禁用 iCloud 同步；活动 `auth.json` 仍是文件凭据。不读取/导出官方 Codex 钥匙串。
- 账号切换仅修改共享目录的认证和允许的路由根设置；主动配置预热时额外维护用户级 `hooks.json`。不移动会话、数据库、插件、skills、项目或工作区。切换 API 后继续原会话可能向新服务商传输历史，切换前必须确认。
- 文件使用当前用户权限、原子替换、排他锁及符号/硬链接检查。它不是抵御同用户恶意程序的沙箱，也无法与不配合的官方进程建立跨程序锁；切换前需退出所有 Codex 实例。
- 恢复只接受切换前/预期切换后的文件状态；发现外部修改会停止，不覆盖未知数据。保留 `pending.enc`，不要手动删除“清理错误”。
- OAuth 子进程只使用独立临时目录；正常完成/取消后清理，进程被强制结束时可能留下敏感临时文件。勿上传 `login-*`、`.enc`、钥匙串导出或登录链接。
- 当前发行仅 ad-hoc 签名，未经 Apple 公证。不要关闭 Gatekeeper、绕过企业策略或盲目授予钥匙串权限。更新后可能需用户重新批准钥匙串访问。
- 本地账号库备份还需要原钥匙串密钥才能恢复；卸载不自动删除共享目录、账号库或密钥。普通删除不保证移除 APFS/Time Machine 副本。

## 漏洞报告

安全漏洞请使用 GitHub 仓库提供的私密漏洞报告入口（如已启用）。没有私密入口时，不要公开凭据或可直接利用的敏感数据；先用不含敏感细节的 Issue 请求私密联系方式。
